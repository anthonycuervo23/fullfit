import 'package:flutter/material.dart';

class FitnessLevelScreen extends StatelessWidget {
  const FitnessLevelScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('FitnessLevelScreen'),
      ),
      body: const Center(
        child: Text('FitnessLevelScreen'),
      ),
    );
  }
}
