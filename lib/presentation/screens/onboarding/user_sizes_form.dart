import 'package:flutter/material.dart';

class UserSizesFormScreen extends StatelessWidget {
  const UserSizesFormScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('UserSizesFormScreen'),
      ),
      body: const Center(
        child: Text('UserSizesFormScreen'),
      ),
    );
  }
}
